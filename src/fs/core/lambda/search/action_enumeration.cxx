
#include <fs/core/lambda/search/action_enumeration.hxx>

namespace lambda::search {

/*
 * TODO: Work in progress
std::shared_ptr<ApplicableActionSet>
NaiveActionEnumerator::applicable(const State& state) const {
    return NaiveIterationApplicableSet(state);
}
*/
} // namespaces
